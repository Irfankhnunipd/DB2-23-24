\subsection{Other Information}

% It is possible to interact with most of the entities (insertion, deletion and update) directly via interface. The only exception is for the session and measurement entities, which are filled through files uploaded via interface.\\

% There are 4 custom enumerations: eventcategories, which contain a value for each possible type of maintenance events. Indicates whether they were ordinary maintenance events, extraordinary and so on. roles (contains a value for each possible role of the users). devicetypes: contains a value for each possible device type (i.e., termometer, dynamometer, barometer …). sessiontypes: contains a value for each type of session (whether the session was a testing, monitoring, or ordinary session).